function printNumbers(start, end) {
    for (let numbers = start; numbers <= end; numbers++)
    console.log(numbers);
}
printNumbers(1, 10);

function printNumbers(start, end) {
    let numbers = start;
    while (numbers <= end) {}
        console.log(numbers);
        numbers++;
}
printNumbers(1, 10);